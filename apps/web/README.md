# Web App

Development mode entry point.

During development, the application runs as:
- Frontend: Vite dev server (http://127.0.0.1:5173)
- Backend: FastAPI with uvicorn (http://127.0.0.1:8000)

Use `scripts/dev.ps1` to start both servers.
